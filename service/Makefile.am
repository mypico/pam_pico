if HAVE_SYSTEMD
systemdsystemunit_DATA = \
	pico-continuous.service
endif

dbuspolicydir=$(sysconfdir)/dbus-1/system.d
dbuspolicy_DATA = uk.ac.cam.cl.pico.service.conf

servicedir = $(datadir)/@PACKAGE@
service_DATA = lock.sh

